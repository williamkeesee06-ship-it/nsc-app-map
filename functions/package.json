{
  "name": "@nsc/functions",
  "version": "0.1.0",
  "private": true,
  "description": "811 automation: ITIC filing bot, Smartsheet write-back, and scheduled sweeps. Deployed as Firebase Cloud Functions (Gen2), separate from the Vercel web/api workspaces.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@sparticuz/chromium": "^131.0.0",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.1",
    "playwright-core": "^1.48.0",
    "smartsheet": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
